# Core computer vision and audio processing
# Python 3.9-3.12 required for MediaPipe compatibility
opencv-python>=4.7.0,<5.0.0
mediapipe>=0.10.0,<0.11.0
numpy>=1.20.0,<2.0.0
librosa>=0.10.0,<1.0.0
scipy>=1.9.0,<2.0.0

# Audio engine (cross-platform)
pyo>=1.0.0,<2.0.0

# TensorFlow (MediaPipe dependency)
# The unified `tensorflow` package supports macOS arm64 natively since 2.16;
# the legacy `tensorflow-macos` package is deprecated and unmaintained.
tensorflow>=2.16.0,<3.0.0

# Windows audio support (optional)
pyaudio>=0.2.11; platform_system == "Windows"